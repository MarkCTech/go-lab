name: CI

on:
  push:
    branches:
      - "**"
  pull_request:

env:
  DOCKER_BUILDKIT: 1
  COMPOSE_DOCKER_CLI_BUILD: 1

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: api/go.mod
          cache-dependency-path: api/go.sum

      - name: Go unit tests
        working-directory: api
        run: go test ./...

      - name: Govulncheck
        working-directory: api
        run: go run golang.org/x/vuln/cmd/govulncheck@v1.1.4 ./...

      - name: Validate OpenAPI spec (Go)
        working-directory: api
        run: go run ./cmd/openapivalidate -spec ../docs/openapi.yaml

  compose-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      actions: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go (schema golden tool)
        uses: actions/setup-go@v5
        with:
          go-version-file: api/go.mod
          cache-dependency-path: api/go.sum

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Select Compose files (GHA cache only when token can write actions cache)
        run: |
          if [ "${{ github.event_name }}" = "push" ] || [ "${{ github.event.pull_request.head.repo.full_name }}" = "${{ github.repository }}" ]; then
            echo "COMPOSE_FILE=docker-compose.yml:docker-compose.ci.yml" >> "$GITHUB_ENV"
          else
            echo "COMPOSE_FILE=docker-compose.yml" >> "$GITHUB_ENV"
          fi

      - name: Prepare env file
        run: cp .env.example .env

      - name: Build images
        run: docker compose build --parallel

      - name: Start stack
        run: docker compose up -d --no-build

      - name: Run migrations
        run: docker compose run --rm migrate

      - name: Wait for backend health
        run: |
          for i in $(seq 1 30); do
            if curl -fsS "http://localhost:5000/readyz" > /dev/null; then
              exit 0
            fi
            sleep 2
          done
          echo "Backend readiness check timed out"
          exit 1

      - name: Verify migration schema golden
        run: go run -C api ./cmd/schemagolden

      - name: Run smoke tests (Go)
        run: go run -C api ./cmd/smoketest -- -base http://localhost:5000

      - name: Dump logs on failure
        if: failure()
        run: |
          docker compose ps
          docker compose logs backend --tail 200
          docker compose logs frontend --tail 200
          docker compose logs mysql --tail 200

      - name: Teardown
        if: always()
        run: docker compose down -v
